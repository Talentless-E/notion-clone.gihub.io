export function getNestedArray(notes) {
  return notes

  //  for (i = 0; i < notes.length; i += 1) {
  //   map[notes[i].id] = i;
  //   //  notes[i].children = [];
    
  // }

  // for (i = 0; i < notes.length; i += 1) {
  //   note = notes[i];
  //   console.log(note);
  //   if (note.parentId !== '-1') {
  //     notes[map[note[i]]]?.children.push(note);
  //   } else {
  //     tree.push(note);
  //   }
  // }
  
  // return tree;
}
